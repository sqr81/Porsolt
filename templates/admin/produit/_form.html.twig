{{ form_start(form) }}

<div class="row mt-4">
    <div class="col-md-6">{{ form_row(form.groupe) }}</div>
    <div class="col-md-6">{{ form_row(form.idProduitPorsolt) }}</div>
</div>
<div class="row">
    <div class="col-md-6">{{ form_row(form.nbreAnimaux) }}</div>
    <div class="col-md-6">{{ form_row(form.voieAdmin) }}</div>
</div>

<div class="row">
    <div class="col-md-6">{{ form_row(form.datePremierPrelevement) }}</div>
</div>

<button class="btn btn-primary">{{ button|default('Sauvegarder') }}</button>

{{ form_end(form) }}

{% extends 'base.html.twig' %}
{% block title 'Groupe index' %}
{% block body %}
    <div class="container">
        {% include '/arrow/arrowEtude.html.twig' %}
    </div>

        <table class="table table-striped table-light mt-4 border border-secondary-rounded text-center">
            <thead>
            <tr>
                <th><button class="btn btn-dark btn-md btn-block ">Groupes</button></th>
                <th><button class="btn btn-dark btn-md btn-block">Id Substance</button></th>
                <th><a href="{{ path('groupe.show',{id: produit.id, slug: produit.slug})  }}" class="btn btn-dark btn-md btn-block">Id Porsolt</a></th>
                <th><button class="btn btn-dark btn-md btn-block">Voie admin</button></th>
                <th><button class="btn btn-dark btn-md btn-block">Date 1er prélèvement</button></th>
                {#<th><a href="{{  path('admin.produit.edit', {id: produit.id}) }}" class="btn btn-dark btn-md btn-block">Nb test item</a></th>#}
            </tr>
            </thead>
            <tbody>
                {% for groupe in produit.groupes %}
                    <tr>
    {#                    <td><a href="{{ path('groupe.show', {id: produit.id, slug: produit.slug}) }}">{{ produit.groupe }}</a></td>#}
                        <td>{{ groupe.intitule }}</td>
                        <td>{{ produit.idProduitPorsolt }}</td>
                        <td>{{ groupe.idAnimalPorsolt }}</td>
                        <td>{{ produit.voieAdmin }}</td>
                        <td>{{ produit.datePremierPrelevement |date("d/m/Y")}}</td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>
        <div class="container">
          <a href="{{ path('admin.groupe.new', {id: produit.id}) }}" class="btn btn-primary">creer</a>
       </div>
        {% endblock %}

